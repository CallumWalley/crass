# CRAAS


```sh
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```


## Docs

### Build Docs

```sh
cd docs && make html
```

### serve Docs Local

```sh
cd docs && ./serve.sh
```